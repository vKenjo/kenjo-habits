# Convex Production Configuration
# Copy this to your deployment platform's environment variables

# Convex Deployment (will be set by `npx convex deploy --prod`)
CONVEX_DEPLOYMENT=prod:your-production-deployment

# Convex URLs (get these after running `npx convex deploy --prod`)
NEXT_PUBLIC_CONVEX_URL=https://your-production-deployment.convex.cloud

# Optional: Convex Site URL
NEXT_PUBLIC_CONVEX_SITE_URL=https://your-production-deployment.convex.site
